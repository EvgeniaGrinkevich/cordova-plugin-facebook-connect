def FACEBOOK_ANDROID_SDK_VERSION = "9.0.0"
dependencies {
    implementation("com.facebook.android:facebook-android-sdk:${FACEBOOK_ANDROID_SDK_VERSION}") {
        exclude group: 'com.google.zxing'
    }
}